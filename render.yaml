services:
  - type: web
    name: meridian-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MCP_SERVER_URL
        value: https://order-mcp-74afyau24q-uc.a.run.app/mcp
      - key: ALLOWED_ORIGINS
        value: "*"
